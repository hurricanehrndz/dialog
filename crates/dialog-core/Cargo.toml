[package]
name = "dialog-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "swiftDialog-compatible contract: CLI parsing, JSON I/O, exit codes, command-file protocol"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
